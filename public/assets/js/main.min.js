const preloader = document.querySelector("[data-preloader]"); window.addEventListener("load", () => { preloader.classList.add("active") }); const cursorDot = document.querySelector("[data-cursor-dot]"), cursorOutline = document.querySelector("[data-cursor-outline]"), anchors = document.querySelectorAll("a"); window.addEventListener("mousemove", e => { let t = e.clientX, a = e.clientY; cursorOutline.animate({ left: `${t}px`, top: `${a}px` }, { duration: 500, fill: "forwards" }) }), document.body.addEventListener("mouseleave", () => { actionOnCursor("add") }), document.body.addEventListener("mouseenter", () => { actionOnCursor("remove") }); const actionOnCursor = e => { cursorOutline.classList[`${e}`]("hide") }; anchors.forEach(e => { e.onmouseover = () => cursorOutline.classList.add("active"), e.onmouseleave = () => cursorOutline.classList.remove("active") }); const navBar = document.querySelector("[data-nav-bar]"), togglers = document.querySelectorAll("[data-toggler]"), overlay = document.querySelector("[data-overlay]"), btnToggler = document.querySelector("[data-btn-toggler]"), toggleNavBar = () => { navBar.classList.toggle("active"), overlay.classList.toggle("active"), document.body.classList.toggle("nav-active"), navBar.classList.contains("active") ? btnToggler.innerHTML = '<i class="fa-solid fa-door-open"></i>' : btnToggler.innerHTML = '<i class="fa-solid fa-door-closed"></i>' }; togglers.forEach(e => { e.addEventListener("click", () => { toggleNavBar() }) }); const header = document.querySelector("[data-header]"); window.onscroll = () => { header.classList[scrollY > 100 ? "add" : "remove"]("active") }; const counter = document.querySelector("[data-counter]"); function setScrollValue() { let e = 100 * scrollY / (document.body.scrollHeight - window.innerHeight); counter.innerText = `${Math.ceil(e)}%` } window.addEventListener("scroll", () => { scrollY > 350 && scrollY < document.body.scrollHeight - 800 ? (counter.classList.add("active"), setScrollValue()) : counter.classList.remove("active") }); const revealElements = document.querySelectorAll("[data-reveal]"), revealDelayElements = document.querySelectorAll("[data-reveal-delay]"), reveal = function () { for (let e = 0, t = revealElements.length; e < t; e++)revealElements[e].getBoundingClientRect().top < window.innerHeight / 1.2 && revealElements[e].classList.add("revealed") }; for (let i = 0, len = revealDelayElements.length; i < len; i++)revealDelayElements[i].style.transitionDelay = revealDelayElements[i].dataset.revealDelay; window.addEventListener("scroll", reveal), window.addEventListener("load", reveal);
